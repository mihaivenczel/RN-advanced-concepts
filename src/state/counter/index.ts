import {counterSelectors} from 'state/counter/counterSelectors';
import {counterActions, counterReducer} from 'state/counter/counterSlice';

export {counterActions, counterReducer, counterSelectors};
